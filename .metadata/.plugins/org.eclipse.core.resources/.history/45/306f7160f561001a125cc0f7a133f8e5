# Configure the microservice Cloud config client.It will allow it to pick configuration 
# from ms-config-server


server:
  port: 8765
spring:
  application:
    name: ms-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-serve