server:
  port: ${port:8888} #By default the config server runs on port 8888.
spring:
  application:
    name: ms-config-server #This is the name of the server which has also been used in application.yml
 
